/* Defines admin TypeScript contracts that keep API payloads, state, and UI props aligned. */
import type { AdminMetric } from '@/shared/types/domain';

export type AdminDashboardMetric = AdminMetric;

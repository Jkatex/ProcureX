/* Provides deterministic identity fixture data for demos and tests that need realistic workflow context. */
export const verificationSteps = ['registry', 'documents', 'capabilities', 'signature'] as const;

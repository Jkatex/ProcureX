/* Renders the procurement Tender Document ProcureX page UI while keeping page-specific presentation near its workflow data. */
import { FirstRunAppPage } from '@/features/workspace/components/procurex/FirstRunAppPage';

export function TenderDocumentProcurexPage() {
  return <FirstRunAppPage page="tender-document" />;
}

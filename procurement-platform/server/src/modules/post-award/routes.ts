import { Router } from 'express';
import { ModuleController } from './controller.js';

export function createModuleRouter() {
  const router = Router();
  const controller = new ModuleController();

  router.get('/', controller.status);
  router.get('/contracts', controller.contracts);
  router.get('/contracts/:contractId/actions', controller.actions);
  router.get('/contracts/:contractId/workspace', controller.workspace);
  router.post('/contracts/:contractId/documents', controller.uploadDocument);
  router.put('/contracts/:contractId/management-plan', controller.upsertManagementPlan);
  router.post('/contracts/:contractId/management-plan/generate-draft', controller.generateManagementPlanDraft);
  router.post('/contracts/:contractId/activation/items/:itemId/submit', controller.submitActivationItem);
  router.post('/contracts/:contractId/activation/items/:itemId/review', controller.reviewActivationItem);
  router.post('/contracts/:contractId/activate', controller.activateContract);
  router.post('/contracts/:contractId/obligations', controller.createObligation);
  router.post('/contracts/:contractId/evidence-requirements', controller.createEvidenceRequirement);
  router.post('/contracts/:contractId/goods/delivery-schedules', controller.createDeliverySchedule);
  router.post('/contracts/:contractId/goods/dispatch-notices', controller.createDispatchNotice);
  router.post('/contracts/:contractId/goods/receipts', controller.createGoodsReceipt);
  router.post('/contracts/:contractId/goods/inspections', controller.createGoodsInspection);
  router.post('/contracts/:contractId/works/site-handovers', controller.createSiteHandover);
  router.post('/contracts/:contractId/works/progress-reports', controller.createWorksProgressReport);
  router.patch('/contracts/:contractId/works/progress-reports/:recordId/review', controller.reviewWorksProgressReport);
  router.post('/contracts/:contractId/works/boq-measurements', controller.createBoqMeasurement);
  router.patch('/contracts/:contractId/works/boq-measurements/:recordId/review', controller.reviewBoqMeasurement);
  router.post('/contracts/:contractId/works/interim-payment-certificates', controller.createInterimPaymentCertificate);
  router.patch('/contracts/:contractId/works/interim-payment-certificates/:recordId/certify', controller.certifyInterimPaymentCertificate);
  router.post('/contracts/:contractId/works/completion-certificates', controller.createWorksCompletionCertificate);
  router.post('/contracts/:contractId/works/defects', controller.createContractDefect);
  router.patch('/contracts/:contractId/works/defects/:recordId/respond', controller.respondToContractDefect);
  router.patch('/contracts/:contractId/works/defects/:recordId/verify', controller.verifyContractDefect);
  router.patch('/contracts/:contractId/works/defects/:recordId/close', controller.closeContractDefect);
  router.post('/contracts/:contractId/services/levels', controller.createServiceLevel);
  router.post('/contracts/:contractId/services/periods', controller.createServicePeriod);
  router.post('/contracts/:contractId/services/reports', controller.createServiceReport);
  router.patch('/contracts/:contractId/services/reports/:recordId/review', controller.reviewServiceReport);
  router.post('/contracts/:contractId/services/credits', controller.createServiceCredit);
  router.patch('/contracts/:contractId/services/credits/:recordId/review', controller.reviewServiceCredit);
  router.post('/contracts/:contractId/services/incidents', controller.createServiceIncident);
  router.patch('/contracts/:contractId/services/incidents/:recordId/respond', controller.respondToServiceIncident);
  router.patch('/contracts/:contractId/services/incidents/:recordId/verify', controller.verifyServiceIncident);
  router.patch('/contracts/:contractId/services/incidents/:recordId/close', controller.closeServiceIncident);
  router.post('/contracts/:contractId/consultancy/deliverables', controller.createConsultancyDeliverable);
  router.patch('/contracts/:contractId/consultancy/deliverables/:recordId/review', controller.reviewConsultancyDeliverable);
  router.post('/contracts/:contractId/consultancy/versions', controller.createDeliverableVersion);
  router.patch('/contracts/:contractId/consultancy/versions/:recordId/review', controller.reviewDeliverableVersion);
  router.post('/contracts/:contractId/consultancy/reviews', controller.createDeliverableReview);
  router.patch('/contracts/:contractId/consultancy/reviews/:recordId/confirm-payment-eligibility', controller.confirmDeliverableReviewPaymentEligibility);
  router.post('/contracts/:contractId/consultancy/final-report', controller.upsertConsultancyFinalReport);
  router.patch('/contracts/:contractId/consultancy/final-report', controller.upsertConsultancyFinalReport);
  router.patch('/contracts/:contractId/consultancy/final-report/:recordId/review', controller.reviewConsultancyFinalReport);
  router.post('/contracts/:contractId/claims', controller.createClaim);
  router.post('/contracts/:contractId/claim-responses', controller.createClaimResponse);
  router.post('/contracts/:contractId/extension-requests', controller.createExtensionRequest);
  router.post('/contracts/:contractId/amendments', controller.createAmendment);
  router.post('/contracts/:contractId/change-requests', controller.createContractChangeRequest);
  router.post('/contracts/:contractId/non-conformances', controller.createNonConformance);
  router.patch('/contracts/:contractId/change-requests/:recordId/review', controller.reviewChangeRequest);
  router.patch('/contracts/:contractId/change-requests/:recordId/respond', controller.respondToChangeRequest);
  router.patch('/contracts/:contractId/change-requests/:recordId/approve', controller.approveChangeRequest);
  router.patch('/contracts/:contractId/change-requests/:recordId/reject', controller.rejectChangeRequest);
  router.patch('/contracts/:contractId/variations/:recordId/review', controller.reviewVariation);
  router.patch('/contracts/:contractId/variations/:recordId/approve', controller.approveVariation);
  router.patch('/contracts/:contractId/variations/:recordId/reject', controller.rejectVariation);
  router.patch('/contracts/:contractId/extension-requests/:recordId/review', controller.reviewExtensionRequest);
  router.patch('/contracts/:contractId/extension-requests/:recordId/approve', controller.approveExtensionRequest);
  router.patch('/contracts/:contractId/extension-requests/:recordId/reject', controller.rejectExtensionRequest);
  router.patch('/contracts/:contractId/claims/:recordId/respond', controller.respondToClaim);
  router.patch('/contracts/:contractId/claims/:recordId/review', controller.reviewClaim);
  router.patch('/contracts/:contractId/claims/:recordId/settle', controller.settleClaim);
  router.patch('/contracts/:contractId/claims/:recordId/reject', controller.rejectClaim);
  router.patch('/contracts/:contractId/claims/:recordId/escalate', controller.escalateClaim);
  router.patch('/contracts/:contractId/non-conformances/:recordId/respond', controller.respondToNonConformance);
  router.patch('/contracts/:contractId/non-conformances/:recordId/verify', controller.verifyNonConformance);
  router.patch('/contracts/:contractId/non-conformances/:recordId/close', controller.closeNonConformance);
  router.post('/contracts/:contractId/securities', controller.createContractSecurity);
  router.patch('/contracts/:contractId/securities/:recordId/review', controller.reviewContractSecurity);
  router.patch('/contracts/:contractId/securities/:recordId/extend', controller.extendContractSecurity);
  router.patch('/contracts/:contractId/securities/:recordId/release', controller.releaseContractSecurity);
  router.patch('/contracts/:contractId/securities/:recordId/claim', controller.claimContractSecurity);
  router.patch('/contracts/:contractId/securities/:recordId/waive', controller.waiveContractSecurity);
  router.post('/contracts/:contractId/penalties', controller.createContractPenalty);
  router.post('/contracts/:contractId/deliverables', controller.createDeliverable);
  router.post('/contracts/:contractId/milestones/:milestoneId/evidence', controller.addMilestoneEvidence);
  router.post('/contracts/:contractId/inspections', controller.createInspection);
  router.post('/contracts/:contractId/acceptances', controller.createAcceptance);
  router.post('/contracts/:contractId/invoices', controller.createInvoice);
  router.put('/contracts/:contractId/three-way-match', controller.upsertThreeWayMatch);
  router.post('/contracts/:contractId/payment-approvals', controller.createPaymentApproval);
  router.patch('/contracts/:contractId/invoices/:invoiceId/verify', controller.verifyInvoiceFinance);
  router.patch('/contracts/:contractId/invoices/:invoiceId/return', controller.returnInvoiceFinance);
  router.patch('/contracts/:contractId/invoices/:invoiceId/reject', controller.rejectInvoiceFinance);
  router.patch('/contracts/:contractId/invoices/:invoiceId/correct', controller.correctInvoiceFinance);
  router.post('/contracts/:contractId/payment-recommendations', controller.createPaymentRecommendation);
  router.patch('/contracts/:contractId/payment-approvals/:recordId/review', controller.reviewPaymentApproval);
  router.patch('/contracts/:contractId/payment-approvals/:recordId/approve', controller.approvePaymentApproval);
  router.patch('/contracts/:contractId/payment-approvals/:recordId/reject', controller.rejectPaymentApproval);
  router.patch('/contracts/:contractId/invoices/:invoiceId/status', controller.updateInvoiceStatus);
  router.post('/contracts/:contractId/payments', controller.createPayment);
  router.patch('/contracts/:contractId/payments/:recordId/initiate', controller.initiatePayment);
  router.patch('/contracts/:contractId/payments/:recordId/complete', controller.completePayment);
  router.post('/contracts/:contractId/payment-confirmations', controller.createPaymentConfirmation);
  router.patch('/contracts/:contractId/payment-confirmations', controller.createPaymentConfirmation);
  router.post('/contracts/:contractId/finance/deductions', controller.createFinanceDeduction);
  router.patch('/contracts/:contractId/finance/deductions', controller.createFinanceDeduction);
  router.post('/contracts/:contractId/finance/retention', controller.createFinanceRetention);
  router.patch('/contracts/:contractId/finance/retention', controller.createFinanceRetention);
  router.post('/contracts/:contractId/finance/advance-recovery', controller.createFinanceAdvanceRecovery);
  router.patch('/contracts/:contractId/finance/advance-recovery', controller.createFinanceAdvanceRecovery);
  router.post('/contracts/:contractId/finance/liquidated-damages', controller.createFinanceLiquidatedDamages);
  router.patch('/contracts/:contractId/finance/liquidated-damages', controller.createFinanceLiquidatedDamages);
  router.post('/contracts/:contractId/issues', controller.createIssue);
  router.post('/contracts/:contractId/variations', controller.createVariation);
  router.post('/contracts/:contractId/disputes', controller.createDispute);
  router.patch('/contracts/:contractId/risks/:recordId/review', controller.reviewRisk);
  router.patch('/contracts/:contractId/risks/:recordId/mitigate', controller.mitigateRisk);
  router.patch('/contracts/:contractId/risks/:recordId/close', controller.closeRisk);
  router.patch('/contracts/:contractId/issues/:recordId/assign', controller.assignIssue);
  router.patch('/contracts/:contractId/issues/:recordId/resolve', controller.resolveIssue);
  router.patch('/contracts/:contractId/issues/:recordId/close', controller.closeIssue);
  router.patch('/contracts/:contractId/disputes/:recordId/respond', controller.respondToDispute);
  router.patch('/contracts/:contractId/disputes/:recordId/resolve', controller.resolveDispute);
  router.patch('/contracts/:contractId/disputes/:recordId/close', controller.closeDispute);
  router.post('/contracts/:contractId/terminations', controller.createTermination);
  router.patch('/contracts/:contractId/terminations/:recordId/respond', controller.respondToTermination);
  router.patch('/contracts/:contractId/terminations/:recordId/decide', controller.decideTermination);
  router.patch('/contracts/:contractId/terminations/:recordId/settle', controller.settleTermination);
  router.patch('/contracts/:contractId/terminations/:recordId/reject', controller.rejectTermination);
  router.patch('/contracts/:contractId/terminations/:recordId/terminate', controller.terminateContract);
  router.patch('/contracts/:contractId/terminations/:recordId/close', controller.closeTermination);
  router.post('/contracts/:contractId/notices', controller.createContractNotice);
  router.patch('/contracts/:contractId/notices/:recordId/acknowledge', controller.acknowledgeContractNotice);
  router.patch('/contracts/:contractId/notices/:recordId/respond', controller.respondToContractNotice);
  router.patch('/contracts/:contractId/notices/:recordId/close', controller.closeContractNotice);
  router.post('/contracts/:contractId/meetings', controller.createContractMeeting);
  router.patch('/contracts/:contractId/meetings', controller.createContractMeeting);
  router.post('/contracts/:contractId/meetings/:recordId/actions', controller.createContractMeetingAction);
  router.patch('/contracts/:contractId/meetings/:recordId/actions', controller.createContractMeetingAction);
  router.patch('/contracts/:contractId/meeting-actions/:recordId/complete', controller.completeContractMeetingAction);
  router.patch('/contracts/:contractId/meeting-actions/:recordId/verify', controller.verifyContractMeetingAction);
  router.patch('/contracts/:contractId/meeting-actions/:recordId/close', controller.closeContractMeetingAction);
  router.put('/contracts/:contractId/warranties', controller.upsertWarranty);
  router.patch('/contracts/:contractId/warranties/:recordId/respond', controller.respondToWarranty);
  router.patch('/contracts/:contractId/warranties/:recordId/verify', controller.verifyWarranty);
  router.patch('/contracts/:contractId/warranties/:recordId/close', controller.closeWarranty);
  router.post('/contracts/:contractId/urgent-actions/recalculate', controller.recalculateUrgentActions);
  router.put('/contracts/:contractId/required-documents', controller.upsertRequiredDocument);
  router.put('/contracts/:contractId/supplier-performance', controller.upsertSupplierPerformance);
  router.get('/contracts/:contractId/closeout/readiness', controller.closeoutReadiness);
  router.patch('/contracts/:contractId/closeout/readiness', controller.closeoutReadiness);
  router.patch('/contracts/:contractId/closeout/steps/:stepId', controller.updateCloseoutStep);
  router.put('/contracts/:contractId/closeout', controller.upsertCloseout);

  return router;
}

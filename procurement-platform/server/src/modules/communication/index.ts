/* Exports the communication server module boundary for route registration and cross-module wiring. */
export { createModuleRouter } from './routes.js';
export { moduleDefinition } from './types.js';

